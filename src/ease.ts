// Same curve Xuro uses everywhere (its src/lib/ease.ts EASE_OUT), duplicated
// here so Tugur's motion matches without depending on Xuro's codebase.
export const EASE_OUT = [0.16, 1, 0.3, 1] as const;
